# WDK Configuration
WDK_SEED=           # 12-word mnemonic seed phrase (NEVER commit the real .env)
WDK_INDEXER_API_KEY= # Optional: for on-chain indexer queries

# Ethereum Sepolia RPC
ETH_RPC_URL=https://sepolia.drpc.org

# USDT Testnet Contract (Sepolia)
USDT_CONTRACT=0xd077a400968890eacc75cdc901f0356c943e4fdb

# Server
PORT=3000
NODE_ENV=development
