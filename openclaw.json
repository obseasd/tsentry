{
  "mcpServers": {
    "tsentry-wdk": {
      "command": "node",
      "args": ["src/mcp-server.js"],
      "env": {
        "WDK_SEED": "${WDK_SEED}",
        "ETH_RPC_URL": "${ETH_RPC_URL}",
        "USDT_CONTRACT": "${USDT_CONTRACT}"
      }
    }
  }
}
